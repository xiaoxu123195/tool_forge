# 默认: 文本文件统一用 LF 存储，避免跨平台(Windows/macOS/Linux) CRLF 混乱
* text=auto eol=lf

# Windows 专属脚本必须保持 CRLF，否则 cmd/powershell 解析会出问题
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# 二进制文件显式标记，防止被当成文本做换行符转换
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.icns  binary
*.webp  binary
*.pdf   binary
*.zip   binary
*.gz    binary
*.7z    binary
*.exe   binary
*.dll   binary
*.so    binary
*.dylib binary
*.wasm  binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.otf   binary
*.eot   binary

# 内置 adb 的载荷。上面的 *.gz 已经覆盖到了，这里再点名一次是因为它特殊：
# 里面是 8MB 的可执行文件，而本机 core.autocrlf=true —— 一旦哪天被当成文本，
# 换行符转换会把 adb.exe 改坏，而且要等到用户那台机器上才看得出来
backend/tools/adbx/bundled/platform-tools.tar.gz binary
